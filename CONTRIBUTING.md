# Contributing

Create a branch, run `make setup`, add tests, then run `make lint`, `make test`, and `make audit`.
Open a pull request using the template and describe security impact and rollback. Releases use
annotated semantic tags such as `v1.2.3`; never publish by manually changing workflow conditions.
Do not commit `.env`, generated artifacts, SBOMs, tokens, or personal data.
